import AuthForm from "../components/auth/AuthForm";

// Sebelumnya file ini masih placeholder ("Form register akan dibangun
// di PHASE 4"). Backend auth sudah siap (PHASE 4 selesai), jadi
// sekarang diisi dengan AuthForm yang sudah terhubung ke
// services/auth.ts.

export default function Register() {
  return <AuthForm register />;
}